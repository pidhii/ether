
let pub empty = of_list [] in

-- TODO: optimize
let pub init n f =
  let rec loop i acc =
    if i < n then
      loop (i + 1) (push acc (f i))
    else acc
  in
  loop 0 empty
in

-- TODO: optimize
let pub make n x =
  init n (const x)
in

nil
